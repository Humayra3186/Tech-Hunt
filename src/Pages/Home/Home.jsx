import React, { useContext } from 'react';
import { AuthContext } from '../../Provider/AuthProvider';

const Home = () => {
  
    return (
        <div>
home
        </div>
    );
};

export default Home;